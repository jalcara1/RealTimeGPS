<!DOCTYPE html>
<html>

  <head>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <style>
      /* Always set the map height explicitly to define the size of the div
      * element that contains the map. */
      #map {
      height: 100%;
      }
      /* Optional: Makes the sample page fill the window. */
      html,
      body {
      height: 100%;
      margin: 0;
      padding: 0;
      }
    </style>
  </head>

  <body>
    <div id="map"></div>
    <script>
      function getLocation() {
      var map = new google.maps.Map(document.getElementById('map'), {
      center: { lat: 6.198053, lng: -75.577884 },
      zoom: 16
      });
      <!-- var posEafit = new google.maps.LatLng(6.20,-75.56); -->
      var marker = new google.maps.Marker({
      position: { lat: 6.19841, lng: -75.579236 },
      map: map,
      title: '<%= user.local.email %>'
      });
      }
      <!-- navigator.geolocation.getCurrentPosition(showPosition); -->
      
      <!-- function showPosition(position) { -->
      <!-- function showPosition(position) { -->
      <!-- centro del mapa -->
      <!-- var centroMapa = new google.maps.LatLng(position.coords.latitude, position.coords.longitude); -->
      <!-- var centroMapa = new google.maps.LatLng(6.20,-75.56); -->
      <!-- var mimapa = new google.maps.Map(document.getElementById("map"),misOpciones); -->
      <!-- Pintar un Marcador (marker) Ubicado en la Universidad EAFIT -->
      <!-- var posEafit = new google.maps.LatLng(position.coords.latitude, position.coords.longitude); -->
      <!-- <\!-- var posEafit = new google.maps.LatLng(6.20,-75.56); -\-> -->
      <!-- var marcador = new google.maps.Marker({ -->
      <!-- position: posEafit, -->
      <!-- map: mimapa, -->
      <!-- title: 'EAFIT' -->
      <!-- }); -->
      <!-- } -->

    </script>
    <!-- actualizar en YOUR_API_KEY la clave propia generada
	 API_KEY = AIzaSyAaMAY9VlVPbr5yPqofvzPVLr4TWUDdZyY -->
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAaMAY9VlVPbr5yPqofvzPVLr4TWUDdZyY&callback=getLocation"
	    async defer></script>
  </body>
</html>
